# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. It treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. No source code, config, tests, or lockfiles were modified, and no builds or tests
were executed. Evidence is cited by `path:line`.

---

## Per-Claim Findings

### 1. What it is
**Status:** `consistent`

**Evidence:** `verified-by-reading-code` — Entry point at `src/main.tsx`, root component
`src/App.tsx` imports all 5 visualization components and matches the README description
(Leaflet heatmap, Recharts charts, vis-network graph, FastAPI sidecar at localhost:8008).
`sidecar/main.py` confirms FastAPI + APScheduler + SQLite backend. README intro is accurate.

---

### 2. Current state
**Status:** `drifted` — fixed in `CLAUDE.md` (both `## Status` section and portfolio-context block)

**Evidence:** `verified-by-reading-code` — CLAUDE.md previously said "Phases 0–2 complete"
but Phase 3 (Polish + Scheduler UX) features are all implemented in the code:

| Phase 3 task | Evidence |
|---|---|
| Filter persistence | `src/App.tsx:65-106` — `filters.json` store load/save via tauri-plugin-store |
| Sync schedule config in SettingsModal | `src/components/settings-modal.tsx:38-40, 103-111` |
| Test connection button | `src/components/settings-modal.tsx:122-156`; backend `sidecar/routers/sync.py:109-123` |
| Empty state for visualizations | `src/components/skill-bar-chart.tsx:60-68` (no-data state with "Sync Now" prompt) |
| Error boundary | `src/components/error-boundary.tsx` — class component with retry button |
| App icon + window title | `src-tauri/tauri.conf.json:5,14-15,28-34` — productName "Job Market Heatmap", icon array defined |
| Schedule reschedule backend | `sidecar/routers/sync.py:94-106`; `sidecar/core/scheduler.py:49-56` |

Release-closeout remains incomplete: `src-tauri/tauri.conf.json:20-22` has `"csp": null`;
`package.json:4` and `src-tauri/tauri.conf.json:3` both show version "0.1.0" (no v1.0.0 bump);
no baseline Rust tests visible in `src-tauri/`.

**Changes made:**
- `CLAUDE.md:60` `## Status` section:
  `Phases 0–2 complete (scaffold, ingestion, 5 visualizations + filter panel).`
  → `Phases 0–3 complete (scaffold, ingestion, 5 visualizations + filter panel, Phase 3 UX polish).`
- `CLAUDE.md:75` portfolio-context `## Current State` block:
  `**Phases 0–2 complete** (scaffold, ingestion pipeline, 5 visualizations + filter panel).`
  → `**Phases 0–3 complete** (scaffold, ingestion pipeline, 5 visualizations + filter panel, Phase 3 UX polish).`

---

### 3. Stack
**Status:** `consistent`

**Evidence:** `verified-by-reading-code`

| Library | Documented | Actual | Source |
|---|---|---|---|
| Recharts | 3 | 3.8.0 | `package.json:21` |
| vis-network | 10 | 10.0.2 | `package.json:22` |
| Leaflet.js | 1.9 | 1.9.4 | `package.json:15` |
| react-leaflet | 4 | 4.2.1 | `package.json:19` |
| APScheduler | 3.11 | 3.11.2 | `sidecar/requirements.txt:5` |
| FastAPI | 0.115 | 0.115.12 | `sidecar/requirements.txt:15` |
| spaCy | 3.8 | 3.8.13 | `sidecar/requirements.txt:48` |
| PyInstaller | 6 | 6.19.0 | `sidecar/requirements.txt:36` |
| tauri-plugin-store | present | "2" | `src-tauri/Cargo.toml:22` |

All documented stack entries match the code.

---

### 4. How to run
**Status:** `drifted` — fixed in `CLAUDE.md`

**Evidence:** `verified-by-reading-code` — CLAUDE.md `## Build / Test / Run` block referenced
`tests/` and `src/` as pytest and ruff targets. No root-level `tests/` directory exists (confirmed
by glob search). Python tests live at `sidecar/tests/` and Python source is directly under
`sidecar/` (no `src/` subdirectory). No `pyproject.toml` or `ruff.toml` was found at either
the repo root or `sidecar/`.

**Changes made:**
- `CLAUDE.md:25`: `python -m pytest tests/ -v` → `python -m pytest sidecar/tests/ -v`
- `CLAUDE.md:26`: `ruff check src/ tests/` → `ruff check sidecar/`

The `pnpm tauri dev` and `pnpm build` commands are consistent with `package.json:6-9`.
README installation commands (`pnpm install`, `pip install -r sidecar/requirements.txt`) are
consistent with the actual repo structure. `python -m spacy download en_core_web_sm` in README
is redundant (the model is already pinned in `sidecar/requirements.txt:14` as a direct URL),
but not incorrect — classified `unverifiable` whether redundancy causes a runtime issue.

---

### 5. Known risks
**Status:** `consistent`

**Evidence:** `verified-by-reading-code` — CLAUDE.md lists the key risks:
- Adzuna credentials stored via `tauri-plugin-store` (not macOS Keychain) — confirmed by
  `src-tauri/src/lib.rs:16-18` (stores to `credentials.json` store file).
- No class components — `src/App.tsx` and all component files use hooks only (confirmed
  `error-boundary.tsx` is the one class component, which is required for React error boundaries).
- FastAPI not called on mount without user action — `src/App.tsx:66-96` gates on `useEffect`
  only for credential hydration, not data fetches; data fetches are query-driven.
- Personal use only — no code distributes or aggregates data.

---

### 6. Next move
**Status:** `consistent` (after phase-status fix above)

**Evidence:** `verified-by-reading-code` — Release-closeout items remain absent from code:
`src-tauri/tauri.conf.json:20-22` has `"csp": null` (CSP not hardened); version is "0.1.0" in
both `package.json:4` and `src-tauri/tauri.conf.json:3`; no Rust test files visible. The
PORTFOLIO-DISPOSITION (`docs/PORTFOLIO-DISPOSITION.md`) accurately captures the next arc
(branch consolidation, v1.0 closeout cadence, sidecar packaging verification, APScheduler-
when-closed posture decision). CLAUDE.md now correctly points to release-closeout as the
remaining work.

---

## Contradictions for Manual Review

These files are outside the editable scope but contain drift that a human should fix:

### `IMPLEMENTATION-ROADMAP.md`

| Location | Issue | Suggested fix |
|---|---|---|
| `IMPLEMENTATION-ROADMAP.md:29` | File structure shows `frontend/src/` as the frontend root; actual source is at `src/` (repo root). | Replace `frontend/` prefix with `src/` throughout the file structure diagram. |
| `IMPLEMENTATION-ROADMAP.md:46` | `lib/api.ts` described as "Typed axios wrapper"; implementation uses native `fetch()` (`src/lib/api.ts:24-31`). | Change to "Typed fetch wrapper for FastAPI endpoints". |
| `IMPLEMENTATION-ROADMAP.md:330-350` | Dependency list includes `npm install axios@1.7`. Axios is not in `package.json` and is not used. | Remove `axios` install line. |
| `IMPLEMENTATION-ROADMAP.md:338` | `vis-data@7` listed; actual version is `8.0.3` (`package.json:21`). | Update to `vis-data@8`. |
| `IMPLEMENTATION-ROADMAP.md` Phase 2/3 tasks | Phase 3 UX polish features (filter persistence, schedule config, test connection, error boundary, app icon) are described as roadmap tasks but are fully implemented. The roadmap's phase-completion status is not explicitly tracked, so no single line to fix — the document's tasks section implies these are still pending. | Add a "✅ Complete" marker or a Phase completion note at the top of each complete phase section. |

### `IMPLEMENTATION-ROADMAP.md` Phase 3 status note
The IMPLEMENTATION-ROADMAP.md has no overall phase-completion tracking. A human should add
a brief completion marker at the top of the Phase 3 section, similar to what Phase 0–2 would
have, since all Phase 3 verification checklist items are now implemented in code.

---

## Footer

- **Date/time:** 2026-06-02 19:48:12 PDT
- **Branch:** `docs/truth-up-2026-06-02`
- **HEAD sha reconciled against:** `35509e0f701569390eedd8f8b6725c1c646e7802`
